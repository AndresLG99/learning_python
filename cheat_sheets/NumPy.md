
# 🔢 NumPy Cheat Sheet  
Clean, Visual & Beginner‑Friendly

---

## 📌 1. Creating Arrays
```python
np.array([1,2,3])
np.zeros((3,3))
np.ones((2,2))
np.random.rand(4)
np.arange(0,10,2)
np.linspace(0,1,5)
```

---

## 📌 2. Array Operations
```python
a + b
a * b
np.dot(a, b)
np.mean(a)
np.std(a)
```

---

## 📌 3. Indexing & Slicing
```python
a[0]
a[1:3]
a[:, 0]
a[0, :]
```

---

## 📌 4. Reshaping
```python
a.reshape(3, 2)
a.flatten()
```

---

## 📌 5. Useful Functions
```python
np.unique(a)
np.sort(a)
np.argmax(a)
np.argmin(a)
```

---

